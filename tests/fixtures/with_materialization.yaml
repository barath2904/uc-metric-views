version: "1.1"
source: analytics.gold.fct_orders
dimensions:
  - name: Order Date
    expr: "o_orderdate"
measures:
  - name: Row Count
    expr: "COUNT(1)"
materialization:
  schedule: every 6 hours
  mode: relaxed
  materialized_views:
    - name: baseline
      type: unaggregated
